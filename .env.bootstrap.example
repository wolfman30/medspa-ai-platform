# Bootstrap Mode Configuration ($20/month stack)
# Copy this to .env for local bootstrap testing

# Application Configuration
PORT=8080
ENV=development
LOG_LEVEL=info
USE_MEMORY_QUEUE=true
WORKER_COUNT=2

# Messaging Provider (auto | telnyx | twilio)
SMS_PROVIDER=auto

# Database Configuration (Use Neon free tier for production, local PG for dev)
# Neon: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/medspa?sslmode=require
DATABASE_URL=postgresql://medspa:medspa@localhost:5432/medspa?sslmode=disable

# Redis Configuration (Local for dev, localhost:6379 on Lightsail VPS)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# Bedrock Configuration (REQUIRED)
# Claude Haiku 4.5 uses inference profiles (cross-region) in Bedrock.
BEDROCK_MODEL_ID=us.anthropic.claude-haiku-4-5-20251001-v1:0
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v1

# Telnyx Configuration (REQUIRED for SMS)
TELNYX_API_KEY=your-telnyx-api-key
TELNYX_MESSAGING_PROFILE_ID=your-messaging-profile-id
TELNYX_WEBHOOK_SECRET=your-webhook-secret
TELNYX_STOP_REPLY=You have been opted out. Reply HELP for info.
TELNYX_HELP_REPLY=Reply STOP to opt out or contact support@medspa.ai.
TELNYX_START_REPLY=You're opted back in. Reply STOP to opt out.
TELNYX_FIRST_CONTACT_REPLY=
TELNYX_VOICE_ACK_REPLY=
TELNYX_RETRY_MAX_ATTEMPTS=5
TELNYX_RETRY_BASE_DELAY=5m
TELNYX_HOSTED_POLL_INTERVAL=15m

# Twilio Configuration (OPTIONAL - legacy support)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WEBHOOK_SECRET= # optional, defaults to TWILIO_AUTH_TOKEN when empty
TWILIO_FROM_NUMBER=
TWILIO_ORG_MAP_JSON={}

# Square Payments Configuration (REQUIRED for deposits)
SQUARE_ACCESS_TOKEN=your-square-access-token
SQUARE_LOCATION_ID=your-square-location-id
SQUARE_WEBHOOK_SIGNATURE_KEY=your-webhook-signature-key
SQUARE_SUCCESS_URL=https://yourdomain.com/booking-success
SQUARE_CANCEL_URL=https://yourdomain.com/booking-cancelled
DEPOSIT_AMOUNT_CENTS=5000

# Dev/demo only: auto-purge configured test numbers after successful Square sandbox payments.
# Comma-separated digits; 10-digit numbers are assumed US and will be prefixed with "1".
SANDBOX_AUTO_PURGE_PHONE_DIGITS=
SANDBOX_AUTO_PURGE_DELAY=0s

# Admin JWT Secret (for admin endpoints)
ADMIN_JWT_SECRET=your-secret-key-change-in-production

# Compliance
QUIET_HOURS_START=21:00
QUIET_HOURS_END=07:30
QUIET_HOURS_TZ=America/New_York

# EMR Integration - Nextech (15% market share, public API)
NEXTECH_BASE_URL=https://api-sandbox.nextech.com  # Change to production when ready
NEXTECH_CLIENT_ID=your-nextech-client-id
NEXTECH_CLIENT_SECRET=your-nextech-client-secret

# EMR Integration - Boulevard (25% market share, enterprise only)
# Contact support@blvd.co for API access
# BOULEVARD_BASE_URL=https://developers.joinblvd.com
# BOULEVARD_API_KEY=...

# EMR Integration - Aesthetic Record (30% market share, requires partnership)
# Contact Aesthetic Record sales for API access
# AESTHETIC_RECORD_BASE_URL=...
# AESTHETIC_RECORD_API_KEY=...

# AWS Configuration (REQUIRED for Bedrock; SQS/Dynamo are not used in bootstrap mode)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
