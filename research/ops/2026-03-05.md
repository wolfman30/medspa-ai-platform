# Ops Report — 2026-03-05

## 01:00 UTC (8:00 PM ET prev day)

### CI Status
- **FAILURE** on commit `711bdea` ("debug: fan-out logging + use service-specific providers for availability")
  - Cause: `go vet` error — `main redeclared in this block` (two `.go` files with `package main` in `scripts/`)
  - **Fix applied**: Moved each script into its own subdirectory (`scripts/seed-knowledge/main.go`, `scripts/test-moxie-avail/main.go`)
  - Fix commit: `b776754`, pushed to main
- Previous CI run (commit before) was ✅ success

### Dev API
- `https://api-dev.aiwolfsolutions.com/ready` → **200 OK** ✅

### Workspace
- Clean — no uncommitted changes

### Deploy Pipeline
- Deploy workflow triggered but skipped (expected — CI failed)
- An in-progress deploy from the prior successful commit was also observed

### Actions Taken
1. Investigated CI failure logs
2. Fixed `main` redeclaration by separating scripts into subdirectories
3. Committed and pushed fix (`b776754`)
